The visual direction and Design System are now approved.

Implement the redesigned UI.

Before modifying each page:

1. Read docs/DESIGN_SYSTEM.md
2. Inspect existing components
3. Reuse existing components where possible
4. Preserve all functionality
5. Preserve existing content/data
6. Preserve routing
7. Preserve API behavior

Implementation requirements:

- Use design tokens
- Use reusable components
- Avoid duplicated CSS
- Avoid arbitrary values
- Avoid one-off components
- Maintain semantic HTML
- Maintain accessibility
- Implement responsive behavior intentionally

After implementation:

1. Review the page visually
2. Compare it against the Design System
3. Identify inconsistencies
4. Fix them
5. Check mobile
6. Check tablet
7. Check desktop

Do not modify unrelated functionality.